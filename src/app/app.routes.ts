import { Routes } from '@angular/router';
import { NewComponent } from './registration/new/new.component';

export const routes: Routes = [
    { path: 'registration/new', component: NewComponent }
];
